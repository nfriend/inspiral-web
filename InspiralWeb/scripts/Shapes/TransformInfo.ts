/// <reference path='../definitions/references.d.ts' />

module InspiralWeb.Shapes {
    'use strict';

    export interface TransformInfo {
        x: number;
        y: number;
        angle: number;
        penX: number;
        penY: number;
    }
}
