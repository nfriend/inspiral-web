How to Install InspiralWebSaver
---------------------------

1. Build in Release mode
1. Copy everything from the solution's `/bin/Release` directory to `C:/Windows/System32`
1. Rename `InspiralWebScreenSaver.exe` to `InspiralWebSaver.scr`
